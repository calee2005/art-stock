{
  "name": "@art-stock/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test src/host.test.ts src/plugins.test.ts && cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri"
  },
  "dependencies": {
    "@art-stock/core": "workspace:*",
    "@art-stock/s3": "workspace:*",
    "@art-stock/ui": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
