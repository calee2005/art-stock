# 艺术资产管理中心

缘起：我有多台电脑和绘画设备，台式机、笔记本、MacBook Pro、Pad。每天往来于工作单位和家之间，携带或使用的设备是不同的，但是我希望能够多设备、多平台共享我所有的绘画资源，包括素材、画稿、笔刷，等等，全部集中管理起来。





思路：

- 本地+远端：本地永远是缓存，远端是最终存储地点；远端可以有多个；远端之间可以同步；本地可以只保留部分远端的数据；远端只需要支持S3协议即可，我的本地NAS也支持S3协议，阿里云OSS也支持S3协议，这样的话我可以做两个远端，一个在家里加速访问，另一个当我外出工作时，可以使用阿里云的服务
- 变体：由于一个画稿可能有多种方向，我希望一个画稿可以像Git那样有版本、分支
- 资料库：抽象一个资料库的结构，资料库中可以创建多层次的文件夹，文件夹中可以保存文件；文件夹和文件都可以打标签；文件有多种类型：画稿、PDF、音频、视频、数据库、Markdown，只要是合理的文件都可以；用户可以创建多个资料库
- 素材库：相当于复制了一套Eagle软件的核心功能，管理图像等素材，打标签，检索；素材库是全局的
- 数据库：需要实现一个多维数据表的能力，允许我通过数据表管理项目中的信息和资产，就像一个可以自由建表的微型关系型数据库一样；数据库包含在资料库中，就像一个文件，我可以打开数据库进行编辑、进行查询、本地副本可以和远程副本进行比较，同步数据
- Markdown文稿：我可以在项目中添加Markdown文稿，采用所见即所得的编辑器方式；需要能够自动解析目录，能够插入素材库中的图片、音频、视频等资源
- PDF查看工具：允许在线查看PDF页面内容，可以翻页
- 思维导图工具：允许我在资料库中创建和编辑思维导图
- 同步策略：比如素材库可以在本地指定一个文件夹保留所有文件的元数据和缩略图，只有需要取回原始文件时，才需要访问远端服务；我还可以设定自动同步一个资料库中的某个具体的文件夹，或者整个资料库本身，可以停止同步
- 需要支持的端：PC（Windows）、macOS、Android Pad、Web
- 提供一个类似Trello的看板工具，这个看板工具里面的内容可以通过客户端进行管理
- 我还有一块ESP32-S3的开发板整机（附带4色墨水屏），我希望项目中还包含一个四色墨水屏的固件，用来展示我的资料库的情况，可以注册多个远端，通过查询远端的文件汇总资料库的信息，比如资料库的数量，最近修改的文件，以及看板中的待办事项；由于是低功耗电子墨水设备，刷新一屏至少需要10s，因此定为每2小时刷新一次，配置可以通过远程进行修改
- 插件系统：允许通过插件扩展支持打开、编辑的文件类型；例如未来我可以扩展支持虚拟出版物的插件
- MCP：将软件的功能暴露成一组Tools，我可以通过接入WorkBuddy之类的Agent工具完成一些基本操作。例如我可以让AI帮我制定项目计划（编写Markdown，设定看板）、让AI帮我搜集资料等等，不限于此
- 采用MonoRepo的形式，编写完善的构建工具
- 客户端采用Tauri+React即可
- 还需要有一个网页端，我会把这个网页端部署到Github Pages上，这样所有人都可以访问了，用户只需要访问这个网站，然后在里面填写远端的地址和key就可以进行访问，可以查看、下载、上传
- 由于有多种设备，需要考虑完善的冲突处理机制



参考：

- 墨水屏设备的参考代码：D:\Projects\youn-ink-fourcolor-firmware

